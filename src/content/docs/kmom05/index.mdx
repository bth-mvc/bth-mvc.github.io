---
title: 'kmom05: Tokenbörsen — anslut och handla'
description: Koppla din server mot den gemensamma tokenbörsen och ta emot webhooks och SSE.
revision:
  2026-06-01: (A, mos) Första versionen.
---

import Figure from '@components/CustomFigure.astro';
import YouTube from '@components/YouTube.astro';

Den här veckan kopplar du din server som *klient* mot den gemensamma tokenbörsen — ett levande system där alla studenter på kursen är aktiva deltagare. Det är ett medvetet skifte: du implementerar inte längre vad läraren specificerar — du integrerar med ett system du inte kontrollerar. Tekniken är känd (REST, webhooks, SSE) men kontexten är ny och motivationen omedelbar: dina ordrar påverkar priser som alla andra studenter ser i realtid.

## Issues

Arbeta igenom issues i ordning. Varje issue är ett steg framåt i integrationen.

Du är välkommen att skriva egna kommentarer i varje issue medan du arbetar — noteringar, frågor eller korta reflektioner om vad du lärde dig. Det hjälper dig att minnas och ger läraren inblick i din process. Stäng ett issue när du anser att det är klart.

| Issue | Namn | Vad du gör |
|---|---|---|
| 05-10 | `api-key` | Konfigurera API-nyckel och börsens bas-URL i `.env`, verifiera med ett första anrop |
| 05-20 | `mock-server` | Kör lokal mock-server för utveckling, koppla om till live-börsen med miljövariabel |
| 05-30 | `market-data` | Hämta tokens och orderbok från börsen, visa i Svelte-frontenden |
| 05-40 | `place-orders` | Implementera köp/säljorder, hantera felfall (otillräckliga medel, okänd token) |
| 05-50 | `webhooks` | Ta emot och HMAC-verifiera webhooks (mock-server lokalt, polling-fallback mot live-börsen) |
| 05-60 | `sse-feed` | Prenumerera på börsens SSE-flöde, relay:a realtidspriser och händelser till Svelte via egen SSE |

Valfria issues — gör dem om du vill utforska vidare. De kan räknas som utökningar i kmom10.

| Issue | Namn | Vad du gör |
|---|---|---|
| 05-65 | `trade-history` | Hämta och visa paginerad handelshistorik med cursor-pagination |

## Föreläsningar

Titta igenom föreläsningarna innan du börjar med respektive issue. Börja med börsserverns API-föreläsning — den ger helheten du behöver för att förstå hur delarna passar ihop.

| Titel | Om |
|---|---|
| [Tokenbörsen — konceptet och API:et](https://bth-mvc.github.io/teacher/kmom05/token-exchange.html) | Börsserverns design, matchningsmotorn, tokentyper, gamification, lokal mock-server |
| [Att konsumera REST-API:er — patterns och fallgropar](https://bth-mvc.github.io/teacher/kmom05/consuming-rest-apis.html) | Headers, felhantering, timeouts |
| [Webhooks — ta emot och verifiera](https://bth-mvc.github.io/teacher/kmom05/webhooks.html) | HMAC-signaturer, idempotens, retry från avsändarens sida |
| [SSE som relay — prenumerera och vidarebefordra](https://bth-mvc.github.io/teacher/kmom05/sse-relay.html) | Börsflöde → studentserver → frontend |

## Studiematerial

**Läsning** — Läs igenom dessa innan du börjar med respektive issue. Det handlar inte om att memorera — det handlar om att få en bild av varför saker fungerar som de gör, så att koden känns logisk snarare än som magi du kopierar.

| Läs innan | Titel | Varför |
|---|---|---|
| Issue 05-50 | [GitHub Docs — Validating webhook deliveries](https://docs.github.com/en/webhooks/using-webhooks/validating-webhook-deliveries) | Förklarar HMAC-signaturverifiering i praktiken — samma mönster som börsen använder |
| Issue 05-60 | [MDN — Server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events) | SSE-formatet, EventSource och reconnect-beteende |

**Referens** — Ha dessa öppna i en flik när du kodar. Du behöver inte läsa dem i förväg — slå upp det du behöver när du behöver det. Det är så erfarna utvecklare arbetar.

| Verktyg | Länk |
|---|---|
| Börsserverns API-dokumentation | Se Swagger-länk i repots README |
| MDN — EventSource | [MDN — EventSource](https://developer.mozilla.org/en-US/docs/Web/API/EventSource) |
| MDN — Using SSE | [MDN — Using server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events) |
| eventsource (npm) | [npmjs.com/package/eventsource](https://www.npmjs.com/package/eventsource) |
| Node.js crypto (HMAC) | [nodejs.org/api/crypto.html](https://nodejs.org/api/crypto.html) |

## Quiz

När du är klar med veckans issues gör du quizen som finns i Canvas.

Quizen för kmom05 testar om du förstår hur API-nyckelautentisering fungerar, vad HMAC-signaturverifiering gör och varför den är obligatorisk, hur SSE-kedjan från börsen till frontenden fungerar, och skillnaden på att bygga ett API och att konsumera ett.

## Redovisningstext

Skriv din redovisningstext i `report/kmom05.md` i ditt repo. Frågorna nedan är en guide — du behöver inte besvara dem en i taget utan får gärna väva in dem i en sammanhängande text. Berätta om hur det kändes att jobba igenom kmomet, om du fick några aha-upplevelser längs vägen och om något var svårt eller förvirrande. Det är den reflektionen som gör redovisningstexten värdefull, inte bara att du listar att du gjort sakerna.

1. Hur autentiserar du dina anrop mot börsen? Var i koden lagras API-nyckeln och varför?

2. Beskriv SSE-kedjan i din implementation. Vad händer när börsen skickar ett nytt pris?

3. Förklara hur webhook-signaturverifiering fungerar. Vad hade hänt om du hoppat över den?

4. Vad är skillnaden på att bygga ett API (kmom01–04) och att konsumera ett API? Vad var oväntat?

5. Vad är din viktigaste insikt (TIL — Today I Learned) från det här kursmomentet?

## Inlämning

1. Kör `npm run sonar` och jämför med din kmom04-scan. Notera förändringar i din redovisningstext.
2. Stäng de GitHub-issues som du har löst klart.
3. Skriv klart din redovisningstext i `report/kmom05.md`.
4. Committa, tagga och pusha:

```bash
git add report/kmom05.md
git commit -m "Add kmom05 report"
git tag kmom05
git push origin main --tags
```



## Kommentarer & Noteringar

<details>
<summary>Planeringsanteckningar för läraren</summary>

### Fokus

Studenten är inte längre ensam i sin sandbox. Deras ordrar påverkar priser som alla andra studenter ser i realtid. Tekniken är känd — REST, webhooks, SSE — men kontexten är ny och motivationen omedelbar.

### Tokenbörsen — konceptet

Alla studenter delar en gemensam börsserver med ett fast antal förskapade tokens:

- Vid **första autentiserade anropet** skapas en plånbok automatiskt med startbalans och initial tokenfördelning
- En **matchningsmotor** parar ihop ordrar automatiskt: lägsta säljpris möter högsta köppris
- Alla affärer är publika — **marknadsflödet** visar i realtid vad som händer

Tokentyper:

| Typ | Exempel | Beteende |
|---|---|---|
| **Stabil** | FIKA, KAFFE | Låg volatilitet, säker hamn |
| **Volatil** | DBW, NODE | Stora prissvängningar, hög risk/reward |
| **Yield** | HODL | Betalar automatisk utdelning till alla innehavare varje timme |

### Börsserverns API (skiss)

```
GET  /assets                 — lista alla tokens + typ + aktuellt pris
GET  /assets/:id/orderbook   — aktiva köp/säljordrar för en token
POST /orders                 — lägg ny order (köp/sälj)
GET  /trades                 — genomförda affärer (paginerat, cursor)
GET  /portfolio              — inloggad students innehav + yield-historik
GET  /leaderboard            — portfoliovärde rankad för alla studenter
SSE  /market/feed            — priser, affärer, marknadshändelser, yield-utbetalningar
```

### Förutsättning

Börsen måste vara driftsatt *innan* kursen startar. Den ska:
- Kräva API-nyckel (en per student)
- Ha rate limiting med tydliga headers (`X-RateLimit-Remaining`, `Retry-After`)
- Ha Swagger/OpenAPI-dokumentation
- Exponera paginerade endpoints med cursor
- Skicka HMAC-signerade webhooks vid ordermatchning
- Streama marknadsflödet via SSE

En lokal mock-server tillhandahålls för att studenter ska kunna testa utan att påverka den gemensamma marknaden under utveckling.

### SSE-kedjan

```
Börsservern  →  SSE-flöde  →  Studentens server  →  SSE  →  Svelte-frontend
```

Studentens server är en *relay* — den prenumererar på börsens flöde och vidarebefordrar relevanta händelser till sin frontend. Det är ett vanligt mönster för att filtrera och aggregera realtidsdata.

### Webhook-leverans — beslutad strategi (2026-06-22)

Webhooks kräver att studentens server är nåbar. Tre alternativ utvärderades:

1. **ngrok/tunnel** — fungerar men lägger till ett externt verktyg och signup
2. **Polling-endpoint** — börsen sparar payloads, studenten pollar `GET /webhooks/pending`
3. **Mock-server lokalt** — mock-servern skickar webhooks direkt till `localhost:3000`

**Beslut: kombination av 1 (mock lokalt) och 2 (polling live).**

- **Under utveckling:** mock-servern (05-20) kör lokalt och skickar webhooks till `localhost:3000`. Ingen tunnel behövs. Studenten implementerar HMAC-verifiering och webhook-hantering — fullt lärandemål.
- **Mot live-börsen:** börsen erbjuder `GET /webhooks/pending` som fallback. Studenten pollar istället för att ta emot push. HMAC-signaturen finns i payload oavsett leveransmetod — verifieringslogiken är identisk.
- **Valfritt:** studenter som vill kan använda ngrok mot live-börsen för den fulla upplevelsen, men det krävs inte.

### Varför HMAC-signaturverifiering är obligatoriskt

Webhooks triggar portfoliouppdateringar. Att verifiera att de faktiskt kom från börsen är inte valet — det är kravet. HMAC-signaturer är mönstret GitHub, Stripe och Slack använder. Verifieringslogiken är identisk oavsett om webhooken levereras via push (mock/ngrok) eller poll (live-fallback).

### Pedagogisk notering

Det gemensamma marknadsflödet gör att studenter naturligt ser varandras arbete. "Varför rör sig FIKA-priset just nu?" skapar diskussioner som inte behöver orkestreras av läraren — ett av de sällsynta momenten där engagemanget driver sig självt.

### Valfria issues — bedömning (2026-06-23)

**05-65 (trade-history, cursor-pagination):** Tillför nytt koncept. Cursor-pagination är det verkliga mönstret för tidsseriedata och dyker upp i de flesta professionella API:er. Värt att behålla som valfri.

**05-70 (socket-client):** Borttagen (2026-06-24). Socket.io är förkunskap från node-kursen och tillför inte tillräckligt. Socket.io mellan containers (sticky sessions, reverse proxy) är ett ops-ämne — noterat i NEXT-COURSE.md.

**OBS:** Quizen nämner cursor-pagination men det täcks bara av den valfria 05-65. Antingen gör 05-65 obligatorisk eller ta bort cursor-frågan från quizen.

### Omfattning — notering (2026-06-23)

Kmom05 har sex obligatoriska issues och är den mest kodintensiva veckan. Studenten bygger: exchange client, mock-integration, marknadsvy, orderläggning, webhook-hantering med HMAC, och SSE-relay. Varje issue introducerar ett nytt koncept. Om det visar sig för tungt kan 05-60 (SSE) flyttas till kmom06 — den är fristående och kräver inte de andra kmom05-issuena för att fungera.

### Öppna frågor

- Hur stor är startbalansen och hur många tokens per student?
- Hur ofta betalar yield-tokens utdelning och hur ofta triggas marknadshändelser?
- Ska portfoliovärdet visas som kursgraf i Svelte-frontenden?
- Hur hanteras det om börsen är nere under kursveckan?

</details>
