---
title: 'kmom06: Tjänster och robusthet'
description: Bygg intelligenta tjänster och gör börskopplingen robust mot fel.
revision:
  2026-06-01: (A, mos) Första versionen.
---

import Figure from '@components/CustomFigure.astro';
import YouTube from '@components/YouTube.astro';

Den här veckan bygger du intelligenta tjänster ovanpå börskopplingen från kmom05. Din server gör nu mer än att relay:a data — den analyserar prisflödet, reagerar på händelser och håller sin integration robust mot fel. Det är steget från "det fungerar" till "det fungerar även när den externa servern beter sig oförutsägbart."

## Issues

Arbeta igenom issues i ordning. Varje issue är ett steg framåt i applikationen.

Du är välkommen att skriva egna kommentarer i varje issue medan du arbetar — noteringar, frågor eller korta reflektioner om vad du lärde dig. Det hjälper dig att minnas och ger läraren inblick i din process. Stäng ett issue när du anser att det är klart.

| Issue | Namn | Vad du gör |
|---|---|---|
| 06-10 | `idempotent-webhooks` | Lagra webhook-ID:n, ignorera dubbletter — samma affär ska aldrig bokföras två gånger |
| 06-20 | `retry` | Exponential backoff vid 429 och nätverksfel mot börsen, respektera `Retry-After`-headern |
| 06-30 | `price-alerts` | Service som bevakar SSE-flödet och triggar notis när ett token-pris passerar ett tröskelvärde |
| 06-40 | `structured-logging` | Strukturerad loggning med Pino — logga händelser, aldrig känslig data (kopplar till A09 från kmom03) |
| 06-50 | `go-live` | Byt från mock till den riktiga börsen, verifiera att allt fungerar, hämta och visa leaderboard |

Valfria issues — gör dem om du vill utforska vidare. De kan räknas som utökningar i kmom10.

| Issue | Namn | Vad du gör |
|---|---|---|
| 06-55 | `order-history` | Visa användarens ordrar med status (pending/matched/cancelled) |
| 06-60 | `portfolio-analytics` | Beräkna avkastning per token, bäst/sämst presterande, total portföljutveckling |

## Föreläsningar

Titta igenom föreläsningarna innan du börjar med respektive issue. Börja med robusthet-föreläsningen — den ger bakgrunden till varför idempotens och retry är nödvändigt.

| Titel | Om |
|---|---|
| Robusthet mot externa API:er | Retry, backoff, timeout, circuit breaker — mönster för att hantera opålitliga beroenden |
| Idempotens — design för upprepade anrop | At-least-once delivery, idempotency keys, när och hur man lagrar webhook-state |
| Bakgrundstjänster i Express | Services som körs kontinuerligt, livscykelhantering, graceful shutdown |
| Strukturerad loggning i produktion | Pino, log levels, vad som loggas och vad som inte får loggas |

## Studiematerial

**Läsning** — Läs igenom dessa innan du börjar med respektive issue. Det handlar inte om att memorera — det handlar om att få en bild av varför saker fungerar som de gör, så att koden känns logisk snarare än som magi du kopierar.

| Läs innan | Titel | Varför |
|---|---|---|
| Issue 06-10 | [MDN — Idempotent](https://developer.mozilla.org/en-US/docs/Glossary/Idempotent) | Förklarar idempotens-konceptet och varför det är viktigt i distribuerade system |
| Issue 06-20 | [AWS — Exponential Backoff and Jitter](https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/) | Förklarar varför enkel retry utan backoff skapar thundering herd-problem |
| Issue 06-40 | [Pino dokumentation — Getting started](https://getpino.io/#/) | Strukturerad loggning med Pino — log levels, serializers, child loggers |

**Referens** — Ha dessa öppna i en flik när du kodar. Du behöver inte läsa dem i förväg — slå upp det du behöver när du behöver det. Det är så erfarna utvecklare arbetar.

| Verktyg | Länk |
|---|---|
| Pino dokumentation | [getpino.io](https://getpino.io/#/) |
| Pino API reference | [getpino.io/#/docs/api](https://getpino.io/#/docs/api) |

## Quiz

När du är klar med veckans issues gör du quizen som finns i Canvas.

Quizen för kmom06 testar om du förstår vad idempotens är och varför det behövs för webhooks, hur exponential backoff fungerar, vad en circuit breaker är, varför känslig data aldrig ska loggas, och vad "at least once delivery" innebär.

## Redovisningstext

Skriv din redovisningstext i `report/kmom06.md` i ditt repo. Frågorna nedan är en guide — du behöver inte besvara dem en i taget utan får gärna väva in dem i en sammanhängande text. Berätta om hur det kändes att jobba igenom kmomet, om du fick några aha-upplevelser längs vägen och om något var svårt eller förvirrande. Det är den reflektionen som gör redovisningstexten värdefull, inte bara att du listar att du gjort sakerna.

1. Vad är idempotens och varför är det viktigt för webhook-hantering? Hur implementerade du det?

2. Beskriv din retry-logik. Vad händer om börsen returnerar 429 tre gånger i rad?

3. Hur fungerar din prisnotifieringstjänst? Vad händer i kedjan från att börsen skickar ett pris till att användaren ser notisen?

4. Vad loggar du och vad loggar du *inte*? Motivera med ett konkret exempel från din kod.

5. Vad är din viktigaste insikt (TIL — Today I Learned) från det här kursmomentet?

## Inlämning

1. Kör `npm run sonar` och jämför med din kmom05-scan. Notera förändringar i din redovisningstext.
2. Stäng de GitHub-issues som du har löst klart.
3. Skriv klart din redovisningstext i `report/kmom06.md`.
4. Committa, tagga och pusha:

```bash
git add report/kmom06.md
git commit -m "Add kmom06 report"
git tag kmom06
git push origin main --tags
```

4. Öppna en pull request från `main` till `bth/submit/kmom06` i ditt repo.

Läraren läser din redovisningstext via `report/kmom06.md` i PR:en.



## Kommentarer & Noteringar

<details>
<summary>Planeringsanteckningar för läraren</summary>

### Fokus

Bygg intelligenta tjänster ovanpå börskopplingen från kmom05. Studentens server gör nu mer än att relay:a data — den analyserar prisflödet, reagerar på händelser och håller sin integration robust mot fel.

### Prisnotifieringstjänsten

Studenten bygger en service som lyssnar kontinuerligt på SSE-flödet från börsen och jämför inkommande priser mot sparade trösklar i MongoDB:

```typescript
// price-alert.service.ts
class PriceAlertService {
  async checkPrice(token: string, price: number): Promise<void> {
    const alerts = await this.alertRepository.findTriggerable(token, price)
    for (const alert of alerts) {
      await this.notifyUser(alert)
      await this.alertRepository.markTriggered(alert.id)
    }
  }
}
```

När ett tröskelvärde passeras skickas notisen till frontenden via studentens egna SSE-anslutning.

### Idempotent webhook-hantering

Webhooks kan levereras mer än en gång — "at least once delivery". Utan idempotens-hantering bokas samma affär dubbelt i portfolion.

```typescript
const existing = await this.webhookLogRepository.findById(webhookId)
if (existing) return // already processed

await this.processOrder(payload)
await this.webhookLogRepository.save(webhookId)
```

### Strukturerad loggning

```typescript
// Rätt
logger.info({ event: 'order_matched', tokenId, userId, amount }, 'Order matched')
logger.warn({ event: 'webhook_duplicate', webhookId }, 'Duplicate webhook ignored')
logger.error({ event: 'exchange_error', status: err.status }, 'Exchange API failed')

// Fel — loggar aldrig
logger.info({ apiKey, jwtToken, password })
```

Koppling till OWASP A09 från kmom03: en logg som läcker API-nycklar eller tokens är en säkerhetsbrist, inte bara dålig praxis.

### Pedagogisk notering

Idempotens är ett begrepp många studenter möter för första gången här. Det är värt att lägga tid på i föreläsning — "at least once delivery" är ett fundamentalt designbeslut i alla distribuerade system och återkommer i meddelandeköer, betalningssystem och mikrotjänstarkitekturer.

Prisnotifieringstjänsten är det roligaste momentet: studenten ser sin egen notis dyka upp i frontenden när priset passerar tröskeln.

### Quiz-mismatch (2026-06-24)

Quizen nämner circuit breaker men det är inte en egen issue längre. Ändra frågan till "beskriv konceptet" (covered i 06-20:s bakgrundstext om exponential backoff) eller ta bort den.

### Öppna frågor

- Ska graceful shutdown vara ett eget issue? Det är viktigt (stäng SSE-anslutningar ordentligt vid `SIGTERM`) men kan också vara en del av issue 06-40 (logging/robusthet).

### Lösta frågor

- ~~Ska portfolio-analytics kopplas mot en ny vy i Svelte-frontenden?~~ Ja — implementerat som stats-sektion på Portfolio-sidan (06-60).
- ~~Circuit breaker som valfri issue?~~ Borttagen (2026-06-25). Retry med backoff (06-20) täcker samma behov. Noterat i outside-scope.mdx.

</details>
