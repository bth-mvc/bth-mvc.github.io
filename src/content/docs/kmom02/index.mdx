---
title: 'kmom02: Lagerarkitektur'
description: Refaktorera från logik-i-routes till controllers, services och repositories.
revision:
  2026-06-01: (A, mos) Första versionen.
---

import Figure from '@components/CustomFigure.astro';
import YouTube from '@components/YouTube.astro';

Den här veckan refaktorerar du koden från kmom01. Logiken som samlades i routes bryts upp i tre separata lager — controllers, services och repositories — och du lägger till strukturerad felhantering, Zod-validering av inkommande data och publika IDs. Veckan avslutas med att du mäter resultatet i SonarQube och jämför med baslinjemätningen från kmom01.

Det här är arkitekturen du bygger. En HTTP-request rör sig nedåt genom lagren — varje lager har ett enda ansvar och vet ingenting om lagren ovanför.

```
HTTP Request
    ↓
Router          (definierar URL-mönster och middleware)
    ↓
Controller      (läser req, skriver res)
    ↓
Service         (affärslogik — inga Express-typer)
    ↓
Repository      (all databaskod — inga Express-typer)
    ↓
MongoDB
    ↓
HTTP Response
```

Issues 02-10 till 02-30 bygger de tre mittlagerna ett i taget. Issues 02-40 till 02-60 förfinar arkitekturen.

## Issues

Arbeta igenom issues i ordning. Varje issue är ett steg framåt i applikationen.

Du är välkommen att skriva egna kommentarer i varje issue medan du arbetar — noteringar, frågor eller korta reflektioner om vad du lärde dig. Det hjälper dig att minnas och ger läraren inblick i din process. Stäng ett issue när du anser att det är klart.

| Issue | Namn | Vad du gör |
|---|---|---|
| 02-10 | `controllers` | Flytta route-logik till controllers — routes pekar bara på controller-funktioner |
| 02-20 | `services` | Flytta affärslogik till services — controllers anropar services |
| 02-30 | `repositories` | Flytta all Mongoose-kod till repositories — services anropar repositories |
| 02-40 | `error-handling` | Custom error-klasser (`AppError`, `ValidationError`, `NotFoundError`), global error handler, 404 fallback |
| 02-50 | `zod-request` | Zod-scheman i controllers för att validera inkommande request-data, typsäkra DTOs |
| 02-70 | `sonar` | Kör SonarQube igen och jämför rapporten med baslinjemätningen från kmom01 |

Valfria issues — gör dem om du vill utforska vidare:

| Issue | Namn | Vad du gör |
|---|---|---|
| 02-60 | `public-id` | Generera publikt ID med `nanoid` i repository-lagret — använd det i URL:er istället för MongoDB `_id` |

## Föreläsningar

Titta igenom föreläsningarna innan du börjar med respektive issue. Börja med lagerarkitektur-föreläsningen — den ger helhetsbilden som gör att de övriga fallen känns naturliga.

| Titel | Om |
|---|---|
| MVC och lagerarkitektur — varför mönstret finns | Problemet med logik-i-routes, separation of concerns |
| Controllers, services och repositories i TypeScript | Konkret refaktorering steg för steg |
| Felhantering som arkitekturell princip | Custom error-klasser, Express fyra-argument-middleware, middleware-ordning |
| Zod för request-validering | `safeParse` i controllers, felformat mot klienten, `z.infer<>` för typsäkra DTOs |
| Publika IDs med nanoid | Varför MongoDB `_id` inte ska exponeras i URL:er, mönstret för att hålla isär intern och extern identifierare |

## Studiematerial

**Läsning** — Läs igenom dessa innan du börjar med respektive issue. Det handlar inte om att memorera — det handlar om att få en bild av varför saker fungerar som de gör, så att koden känns logisk snarare än som magi du kopierar.

| Läs innan | Titel | Varför |
|---|---|---|
| Issue 02-10 | [Presentation Domain Data Layering — Martin Fowler](https://martinfowler.com/bliki/PresentationDomainDataLayering.html) | Förklarar problemet som lagerarkitektur löser och varför separationen är värd kostnaden — ger ett språk för det du bygger |
| Issue 02-30 | [Mongoose — Schemas](https://mongoosejs.com/docs/guide.html) | Förklarar scheman, sub-dokument och `lean()` — de delar av Mongoose du använder när du skapar Portfolio-modellen |
| Issue 02-40 | [Express — Error handling](https://expressjs.com/en/guide/error-handling.html) | Förklarar hur fyra-argument-middleware fungerar och varför ordningen i middleware-kedjan spelar roll |
| Issue 02-50 | [Zod — Parsing](https://zod.dev/?id=safeparse) | `safeParse` vs `parse` — viktigt skifte när input kommer från opålitliga källor |

**Referens** — Ha dessa öppna i en flik när du kodar. Du behöver inte läsa dem i förväg — slå upp det du behöver när du behöver det. Det är så erfarna utvecklare arbetar.

| Verktyg | Länk |
|---|---|
| Zod dokumentation | [zod.dev](https://zod.dev) |
| nanoid | [npmjs.com/package/nanoid](https://www.npmjs.com/package/nanoid) |
| Express 5.x API | [expressjs.com/en/5x/api.html](https://expressjs.com/en/5x/api.html) |
| Mongoose API | [mongoosejs.com/docs/api](https://mongoosejs.com/docs/api/model.html) |

## Quiz

När du är klar med veckans issues gör du quizen som finns i Canvas.

Quizen för kmom02 testar om du förstår ansvaret för varje lager i lagerarkitekturen, vad en global error handler gör, varför `safeParse` används i controllers, vad `z.infer<>` är, och vad skillnaden är på intern och extern identifierare.

## Redovisningstext

Skriv din redovisningstext i `report/kmom02.md` i ditt repo. Frågorna nedan är en guide — du behöver inte besvara dem en i taget utan får gärna väva in dem i en sammanhängande text. Berätta om hur det kändes att jobba igenom kmomet, om du fick några aha-upplevelser längs vägen och om något var svårt eller förvirrande. Det är den reflektionen som gör redovisningstexten värdefull, inte bara att du listar att du gjort sakerna.

1. Beskriv skillnaden mellan controller, service och repository med ett konkret exempel från din kod. Vad ansvarar varje lager för?

2. Hur fungerar din globala error handler? Vad händer med ett fel som kastas i ett repository?

3. Varför valideras inkommande data i controller-lagret och inte i service-lagret?

4. Du använde Zod i kmom01 för att validera miljövariabler. Vad är skillnaden mot att använda Zod för att validera en request body i en controller?

5. Jämför din SonarQube-rapport från kmom01 med rapporten efter kmom02. Vad förändrades och varför?

6. Vad är din viktigaste insikt (TIL — Today I Learned) från det här kursmomentet?

## Inlämning

1. Stäng de GitHub-issues som du har löst klart.
2. Skriv klart din redovisningstext i `report/kmom02.md`.
3. Committa, tagga och pusha:

```bash
git add report/kmom02.md
git commit -m "Add kmom02 report"
git tag kmom02
git push origin main --tags
```



## Kommentarer & Noteringar

<details>
<summary>Planeringsanteckningar för läraren</summary>

### Fokus

Kursens kärna. Refaktorera från logik-i-routes till ett rent lagersystem. Det pedagogiska momentet är att studenten *redan har sett problemet* (routes som svämmar över från kmom01) innan lösningen presenteras.

Arkitekturen som introduceras:

```
HTTP request
   ↓
Router (route-definition only)
   ↓
Controller (req/res, validering med Zod)
   ↓
Service (affärslogik, vet ingenting om req/res)
   ↓
Repository (all databas-kod, vet ingenting om Express)
   ↓
Response (DTO)
```

### Vad frontenden låser upp

Ingenting nytt — och det är poängen. Portfolioöversikten och auth-formulären som fungerade i kmom01 ska fortsätta fungera efter refaktoreringen. Frontenden är det implicita regressionstestet: om den slutar fungera bröt studenten något i refaktoreringen.

### Pedagogisk notering

Auth-koden i templaten är kursens referensimplementation av lagerarkitekturen. Innan studenten börjar refaktorera sin egen kod bör läraren gå igenom auth-koden i föreläsning och visa hur `auth.controller.ts`, `auth.service.ts` och `user.repository.ts` hänger ihop. Studenten har då ett konkret, fungerande exempel att jämföra med — inte bara ett abstrakt diagram.

Refaktoreringen i kmom02 är därmed inte en övning i att uppfinna mönstret, utan i att applicera ett mönster studenten redan sett i praktiken.

Läraren bör i föreläsningen visa refaktoreringen som en levande process: börja med en route full av logik och bryt upp den steg för steg live. Det är svårt att förmedla *varför* i text.

### SonarQube — andra mätpunkten

När refaktoreringen är klar kör studenten `npm run sonar` igen och jämför rapporten med baslinjemätningen från kmom01. Förbättringen i siffror (komplexitet, duplicering, code smells) är det konkreta beviset på att lagerarkitektur inte bara är teori.

SonarLint i editorn introduceras i kmom04 när hela kvalitetskedjan formaliseras — SonarLint ger löpande feedback, `npm run sonar` ger djupare analys on-demand, och pre-push hook blockerar push om quality gate inte passerar. Att dela upp kedjan med SonarLint i kmom02 och resten i kmom04 splittrar storyn i onödan.

### Publikt ID med nanoid

Routes i node-kursen använde MongoDB `_id` direkt i URL:er. Det läcker implementationsdetaljer och gör IDs instabila vid schema-migrationer. Nu när repository-lagret finns är det rätt plats att lösa det:

```typescript
import { nanoid } from 'nanoid'

// I repository, vid create:
const publicId = nanoid()   // → "V1StGXR8_Z5jdHi6B-myT"
```

Mongoose-dokumentet får ett extra fält `publicId`. Routes och controllers exponerar bara `publicId` — aldrig `_id`.

### Öppna frågor

- Ska DTOs vara egna klasser/typer eller räcker det med Zod-inferens (`z.infer<typeof schema>`)?

</details>
